using System;
using System.Collections.Generic;
using System.Text;

namespace ShopProject.Application.DataTransfer
{
    // The cart owner is always the acting user (IApplicationActor) and the
    // timestamps are set by the server, so the client sends nothing.
    public class CartDto
    {
    }
}
